using System.Windows;
using System.Windows.Controls;

namespace Srk.BetaseriesApiApp.Views {
    /// <summary>
    /// Interaction logic for ShowInfo.xaml
    /// </summary>
    public partial class ShowInfo : UserControl {
        public ShowInfo() {
            InitializeComponent();
        }

        private void episodeMenu_Comments_Click(object sender, RoutedEventArgs e) {
            //if (lvEpisodes.SelectedItem != null) {
            //    var episode = lvEpisodes.SelectedItem as Episode;
            //    tcMain.SelectedItem = tabComments;
            //    Model.EpisodesComments.MyShow = Model.ShowsSearch.SelectedShow.Url;
            //    Model.EpisodesComments.MySeason = episode.Season;
            //    Model.EpisodesComments.MyEpisodeNB = episode.EpisodeNumber;
            //}
        }
    }
}
